/**
 * Shared layout for Master Plot 1 & 2 when shown side-by-side (particle-master tab).
 */

/** Canvas height in CSS pixels — keep identical so plots align visually. */
export const MASTER_PLOT_CHART_HEIGHT_PX = 520;
